/**
 * DynamoDB
 *
 * - method - checkLatest - Calls dynamoDB to fetch the up-to-date date for each services
 * - method - update - if anyone of the *latest* is not up-to-date with the
 * current get items, then updates the dynamoDB.
 * -
 */
